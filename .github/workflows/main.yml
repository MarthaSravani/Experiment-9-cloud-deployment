name: Simple CI Pipeline

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Run basic checks
      run: echo "✅ Code received and CI pipeline triggered"

    - name: Simulate testing
      run: echo "🧪 Running tests (no actual tests configured)"

    - name: Simulate build
      run: echo "🔧 Build step completed (no actual build required)"

    - name: Complete
      run: echo "🎉 CI pipeline completed successfully!"
